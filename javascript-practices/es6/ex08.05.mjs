/**
 * named & unnamed
 */

 const add = function(a, b) {
    return a + b;
}

const subtract = function(a, b) {
    return a - b;
}

const devide = function(a, b) {
    return a / b;
}

export {add, subtract, devide}

export default {add, subtract}